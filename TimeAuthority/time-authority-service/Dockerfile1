FROM node:20-alpine

WORKDIR /app

# install dependencies
COPY package.json package-lock.json* ./
RUN npm ci --production || npm install --production

# copy source
COPY . .

EXPOSE 3000
CMD ["npm", "start"]
